import finalsteps
import loadingscripts

#model1 is the top performing model currently

# command to preprocess images before classification is,
# finalsteps.preprocessor(pather="where the folder is", writeto="where you want the preprocessed images located and folder named")

loadingscripts.model1ui()

#loadingscripts.model1(predict=True, pather="digits/siffrorsvåra")